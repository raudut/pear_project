{% if is_granted('ROLE_ADMIN') -%}
    {% set extendedTemplate = "base_admin.html.twig" %}
{% elseif is_granted('ROLE_LENDER') -%}
    {% set extendedTemplate = "base_lender.html.twig" %}
{% else %}
    {% set extendedTemplate = "base_user.html.twig" %}
{%- endif %}

{% extends extendedTemplate %}

 {% block title %}Liste de mes emprunts{% endblock %}
 
    {% block body %}
    	<table class="table table-hover">
            <thead>
            <tr>
                <th> Id </th>
    			<th> User </th>
    			<th> Produit </th>
    			<th> Date de début </th>
				<th> Date de fin </th>
            </tr>
            </thead>

            <tbody>
    		{%for bo in listBorrowing %}
			<tr>
                <td> {{ bo.id}} </td>
                <td>{{ bo.idUser }}</td>
    			<td>{{ bo.idProduct }}</td>
    			<td>{{ bo.dateDebut|date('Y-m-d') }}</td>
                <td>{{ bo.dateFin|date('Y-m-d') }}</td>
				<td>
                    <button type="button" class="btn btn-outline-secondary" onclick="self.location.href='/rendre-product/{{bo.id}}'">Rendre</button>
                </td>
			</tr>    			
    		{% endfor %}
            </tbody>
    	</table>

{% endblock %}
